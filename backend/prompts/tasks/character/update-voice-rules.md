你是一名对话风格编辑。请从下列**已定稿正文**中，为指定角色归纳 2-3 条可执行的**对话特征规则**（对齐 ainovel-cli CharacterVoice：每条 ≤30 字，从原文归纳而非编造）。

【角色名】{name}
【已有规则（可在其基础上修订，勿无故推翻已稳定的口吻）】
{existing_rules}

【正文节选（含该角色对白）】
{chapter_excerpts}

请输出 JSON（不要使用 markdown 代码块）：
{{
  "name": "{name}",
  "voice_rules": ["规则1", "规则2", "规则3"]
}}

要求：
1. voice_rules 2-3 条，每条 ≤30 字，描述用词/句长/口头禅/潜台词习惯等
2. 必须能从节选中找到依据；若该角色几乎无对白，保留已有规则并最多补 1 条基于行为的弱推断
3. 仅返回 JSON
