请从以下开书共创草稿中提取「全书弱指南针」（StoryCompass）。这是故事倾向指引，不是逐章大纲，也不是固定结局剧本。

只返回 JSON（不要 markdown 代码围栏）：

{{
  "ending_direction": "终局方向倾向（主题性、可演化；若草稿未定可写「待定：…」或短句倾向；150字内）",
  "open_threads": ["需长期推进的主线/长线1", "长线2"],
  "estimated_scale": "模糊规模（短篇连载 / 中长篇 / 预计约 N 卷等，勿写死必须第几章完结）",
  "tone_notes": "调性与禁区摘要（一两句，可空字符串）"
}}

要求：
1. 严格依据共创草稿，不要发明草稿未提及的重大设定或新开篇事件
2. open_threads 2-6 条；信息不足时可少，不要凑无关条目
3. 不要输出固定结局剧情步骤，不要输出章节目录
4. 仅返回 JSON

共创草稿：
{draft}
